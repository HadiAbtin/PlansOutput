# راهنمای تبدیل داده‌ها به Markdown

## مشکل

وقتی داده‌ها از JSON یا string literal کپی می‌شوند، ممکن است `\n` به صورت literal (متن) باشد نه خطوط جدید واقعی. این باعث می‌شود فایل Markdown به درستی نمایش داده نشود.

## راه‌حل‌ها

### روش 1: استفاده از اسکریپت `fix_newlines.py`

اگر فایل را قبلاً ایجاد کرده‌اید و مشکل دارد:

```bash
python3 fix_newlines.py plans/plan-6.md
```

یا برای چند فایل:

```bash
python3 fix_newlines.py plans/plan-6.md plans/plan-7.md
```

### روش 2: استفاده از اسکریپت `convert_data_to_markdown.py`

اگر داده‌های خام (JSON یا string literal) دارید و می‌خواهید به Markdown تبدیل کنید:

```bash
# تبدیل فایل JSON به Markdown
python3 convert_data_to_markdown.py data.json plans/plan-6.md

# یا اگر فایل متنی با \n literal دارید
python3 convert_data_to_markdown.py raw_data.txt plans/plan-6.md
```

### روش 3: استفاده مستقیم در Python

اگر می‌خواهید در کد خودتان این تبدیل را انجام دهید:

```python
import ast

# اگر داده به صورت string literal است
content = '# Plan - Nutrition & Workout Plan\\n\\n## 🥗 Nutrition Plan\\n...'
content = content.encode().decode('unicode_escape')

# یا
content = content.replace('\\n', '\n')
```

### روش 4: استفاده از VS Code / Editor

1. فایل را در ویرایشگر باز کنید
2. `Ctrl+F` (یا Cmd+F در Mac) را بزنید
3. در حالت "Regex" جستجو کنید: `\\n`
4. جایگزین کنید با: `\n` (خط جدید واقعی)
5. یا از Find & Replace استفاده کنید

## پیشگیری از مشکل

### هنگام کپی کردن داده:

1. **از JSON استفاده کنید**: اگر داده JSON است، از `json.dumps()` با `indent` استفاده کنید
2. **از Python string استفاده کنید**: اگر در Python هستید، از f-strings یا triple quotes استفاده کنید
3. **مستقیماً در فایل بنویسید**: به جای کپی کردن string literal، مستقیماً محتوا را در فایل بنویسید

### مثال درست:

```python
# ✅ درست
content = """# Plan - Nutrition & Workout Plan

## 🥗 Nutrition Plan

### Week 1
"""

# ❌ اشتباه
content = "# Plan - Nutrition & Workout Plan\\n\\n## 🥗 Nutrition Plan\\n..."
```

## بررسی سریع

برای بررسی اینکه فایل مشکل دارد یا نه:

```bash
# اگر تعداد \n literal بیشتر از خطوط جدید واقعی باشد، مشکل دارد
grep -c '\\n' plans/plan-6.md  # تعداد \n literal
wc -l plans/plan-6.md          # تعداد خطوط واقعی
```

اگر عدد اول بیشتر از دوم بود، فایل نیاز به fix دارد.

## نکات

- همیشه بعد از paste کردن داده، فایل را در GitHub Preview بررسی کنید
- اگر فایل به صورت یک خط طولانی نمایش داده می‌شود، مشکل newline دارد
- اسکریپت‌ها را executable کنید: `chmod +x fix_newlines.py`

